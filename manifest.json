{
	"manifest_version": 2,

	"name": "Bookmarks downloader",
	"description": "Bookmarks downloader",
	"version": "1.0",
	"background": {
		"scripts": ["background.js"]
	},

	"permissions": ["bookmarks"],
	"browser_action": {
		"default_icon": "favicon.ico",
		"default_title": "Archive now!"
	}
}